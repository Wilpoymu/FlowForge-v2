# Skill Registry — FlowForge-v2

> Auto-generated by sdd-init. Re-run `/sdd-init` to refresh.
> Last updated: 2026-05-14

---

## User Skills

| Skill | Location | Trigger |
|-------|----------|---------|
| `issue-creation` | `~/.config/opencode/skills/issue-creation/SKILL.md` | Creating GitHub issue, reporting bug, requesting feature |
| `branch-pr` | `~/.config/opencode/skills/branch-pr/SKILL.md` | Creating pull request, opening PR, preparing changes for review |
| `judgment-day` | `~/.config/opencode/skills/judgment-day/SKILL.md` | "judgment day", "doble review", "juzgar", "que lo juzguen" |
| `skill-creator` | `~/.config/opencode/skills/skill-creator/SKILL.md` | Creating new AI skill, adding agent instructions, documenting patterns for AI |
| `go-testing` | `~/.config/opencode/skills/go-testing/SKILL.md` | Writing Go tests, Bubbletea TUI testing (not applicable to this project) |

---

## Project Conventions

No convention files found (no AGENTS.md, CLAUDE.md, GEMINI.md, .cursorrules, .agent/skills/).

### Detected Patterns (from codebase)

- **Python**: Pure stdlib + `requests` + `websockets` — no build system, no type hints
- **JavaScript**: Vanilla (no bundler, no TypeScript, no npm) — Chrome Manifest V3
- **Naming**: `_private_variable` convention for internal/global state, snake_case for Python
- **Comments/Messages**: Spanish
- **No test framework**
- **No linter/formatter**
- **No CI**

---

## Compact Rules

### Python
- Use `requests` for HTTP, `websockets` for WS
- Global state uses `_private_variable` + `threading.Lock()` pattern
- Daemon threads for servers, `ThreadPoolExecutor` for parallel work
- `_log()` utility for consistent timestamped logging

### JavaScript (Extension)
- No ES modules — `var` declarations, `function` syntax
- `window.postMessage()` for ISOLATED→MAIN world communication
- Self-contained Chrome extension — no external dependencies
- reCAPTCHA enterprise via `grecaptcha.enterprise`

### Dashboard (HTML+JS)
- Vanilla only — no frameworks, no CDN, no npm
- Dark theme: `#0d0d1a` background, `#1a1a2e` cards, `#ff4444` accent
- CSS inline or `<style>`, JS inline or `<script src="app.js">`
- SSE for real-time updates via `/api/events`
